var skins = ["test","aquarius","pisces","aries","taurus","gemini","cancer","leo","virgo","libra","scorpio","sagittarius","capricorn"];
var skin = 0;
function demo(){
  instances = new Array();
  constellationPreset('aquarius',skins[0]);
  constellationPreset('pisces',skins[0]);
  constellationPreset('aries',skins[0]);
  constellationPreset('taurus',skins[0]);
  constellationPreset('gemini',skins[0]);
  constellationPreset('cancer',skins[0]);
  constellationPreset('leo',skins[0]);
  constellationPreset('virgo',skins[0]);
  constellationPreset('libra',skins[0]);
  constellationPreset('scorpio',skins[0]);
  constellationPreset('sagittarius',skins[0]);
  constellationPreset('capricorn',skins[0]);


};



demo();








// window.addEventListener("keydown",function (evt) {
// if(event.keyCode == 32) {
// 	demo();
// }
// });

// starDensity,
// starRadius,
// starRadiusJitter,
// starVelocity,
// starVelocityJitter,
// connectionRadius,
// connectionWidth,
// connectionOpacity,
// revealRadius,
// backgroundHue,
// backgroundSaturation,
// backgroundLightness,
// foregroundHueJitter,
// foregroundSaturationJitter,
// foregroundLightnessJitter,
// responsive

// foregroundHue,
// foregroundSaturation,
// foregroundLightness,
